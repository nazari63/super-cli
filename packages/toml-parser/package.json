{
    "name": "toml-parser",
    "version": "0.0.1",
    "main": "./index.js",
    "scripts": {
        "build": "napi build --release",
        "test": "cargo test",
        "lint": "cargo fmt --check",
        "lint:fix": "cargo fmt && cargo fmt --check"
    },
    "dependencies": {
        "@napi-rs/cli": "^2.18.4"
    }
}
